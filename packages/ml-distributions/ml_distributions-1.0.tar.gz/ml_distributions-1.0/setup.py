from setuptools import setup

setup(name='ml_distributions',
      version='1.0',
      description='Gaussian & Binomial distributions',
      packages=['ml_distributions'],
      author='Shivam Agarwal',
      zip_safe=False)
