from setuptools import setup

setup(name='first_dist_pack_2',
      version='0.2',
      description='Gaussian and Binomial distributions',
      packages=['first_dist_pack_2'],
      zip_safe=False)
