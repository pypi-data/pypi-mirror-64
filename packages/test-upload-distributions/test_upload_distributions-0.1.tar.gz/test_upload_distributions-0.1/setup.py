from setuptools import setup

setup(name='test_upload_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['test_upload_distributions'],
      zip_safe=False)
