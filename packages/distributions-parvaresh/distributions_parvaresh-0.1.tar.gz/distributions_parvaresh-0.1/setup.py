from setuptools import setup

setup(name='distributions_parvaresh',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_parvaresh'],
      author='Stephen Parvaresh',
      zip_safe=False)
