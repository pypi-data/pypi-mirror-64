from setuptools import setup

setup(name='pair_in_list',
      version='0.2',
      description='Class including two functions to check if there are two elements in a list that add up to a specified sum',
      packages=['pair_in_list'],
      autho='Alex YU',
      zip_safe=False)
