from setuptools import setup

setup(name='probability_and_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['probability_and_distributions'],
      zip_safe=False)
